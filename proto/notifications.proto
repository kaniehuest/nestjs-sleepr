syntax = "proto3";

package notifications;

service Notifications {
    rpc NotifiEmail (NotifyEmailMessage) returns (Empty) {}
}

message Empty  {}

message NotifyEmailMessage {
    string email = 1;
    string text = 2;
}